---
# Source: application-generator/templates/stage.yaml
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-frigate
  namespace: homelab-apps
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: frigate
    sources:
      stages:
      - test-frigate
  promotionTemplate:
    spec:
      vars:
      - name: gitRepo
        value: https://github.com/syoder89-homelab/homelab-apps
      - name: targetBranch
        value: env/prod/frigate
      - name: path_src
        value: ./src
      - name: path_out
        value: ./out
      steps:
      - task:
          name: prepare-workdir 
      # The config-generator chart needs access to the config manifests
      # Copy config manifests into config-generator chart
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/platform
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/applications/frigate/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/application
      # Render the config files from templates
      - uses: helm-template
        config:
          releaseName: config-generator
          path: ${{ vars.path_src }}/charts/config-generator
          outLayout: flat
          outPath: /tmp/values.yaml
          setValues:
            - key: stageName
              value: prod
            - key: envType
              value: prod
            - key: appName
              value: frigate
          #valuesFiles:
          #- ${{ vars.path_src }}/config/prod/frigate-values.yaml
          #- ${{ vars.path_src }}/applications/frigate/values/prod.yaml
      - uses: helm-template
        config:
          releaseName: frigate
          namespace: frigate
          buildDependencies: true
          includeCRDs: true
          path: ${{ vars.path_src }}/applications/frigate
          outPath: ${{ vars.path_out }}/deploy/frigate
          valuesFiles:
          - /tmp/values.yaml
      - task:
          name: push-manifests
        as: promotion
        vars:
        - name: asPR
          value: "true"
        - name: targetBranch
          value: env/prod/frigate
      - uses: argocd-update
        config:
          apps:
          - name: prod-frigate
            sources:
            - repoURL: ${{ vars.gitRepo }}
              desiredRevision: ${{ outputs.promotion.commit }}
---
# Source: application-generator/templates/stage.yaml
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-grafana-scott
  namespace: homelab-apps
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: grafana-scott
    sources:
      stages:
      - test-grafana-scott
  promotionTemplate:
    spec:
      vars:
      - name: gitRepo
        value: https://github.com/syoder89-homelab/homelab-apps
      - name: targetBranch
        value: env/prod/grafana-scott
      - name: path_src
        value: ./src
      - name: path_out
        value: ./out
      steps:
      - task:
          name: prepare-workdir 
      # The config-generator chart needs access to the config manifests
      # Copy config manifests into config-generator chart
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/platform
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/applications/grafana-scott/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/application
      # Render the config files from templates
      - uses: helm-template
        config:
          releaseName: config-generator
          path: ${{ vars.path_src }}/charts/config-generator
          outLayout: flat
          outPath: /tmp/values.yaml
          setValues:
            - key: stageName
              value: prod
            - key: envType
              value: prod
            - key: appName
              value: grafana-scott
          #valuesFiles:
          #- ${{ vars.path_src }}/config/prod/grafana-scott-values.yaml
          #- ${{ vars.path_src }}/applications/grafana-scott/values/prod.yaml
      - uses: helm-template
        config:
          releaseName: grafana-scott
          namespace: scott-monitoring
          buildDependencies: true
          includeCRDs: true
          path: ${{ vars.path_src }}/applications/grafana-scott
          outPath: ${{ vars.path_out }}/deploy/grafana-scott
          valuesFiles:
          - /tmp/values.yaml
      - task:
          name: push-manifests
        as: promotion
        vars:
        - name: asPR
          value: "true"
        - name: targetBranch
          value: env/prod/grafana-scott
      - uses: argocd-update
        config:
          apps:
          - name: prod-grafana-scott
            sources:
            - repoURL: ${{ vars.gitRepo }}
              desiredRevision: ${{ outputs.promotion.commit }}
---
# Source: application-generator/templates/stage.yaml
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-grafana-taylor
  namespace: homelab-apps
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: grafana-taylor
    sources:
      stages:
      - test-grafana-taylor
  promotionTemplate:
    spec:
      vars:
      - name: gitRepo
        value: https://github.com/syoder89-homelab/homelab-apps
      - name: targetBranch
        value: env/prod/grafana-taylor
      - name: path_src
        value: ./src
      - name: path_out
        value: ./out
      steps:
      - task:
          name: prepare-workdir 
      # The config-generator chart needs access to the config manifests
      # Copy config manifests into config-generator chart
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/platform
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/applications/grafana-taylor/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/application
      # Render the config files from templates
      - uses: helm-template
        config:
          releaseName: config-generator
          path: ${{ vars.path_src }}/charts/config-generator
          outLayout: flat
          outPath: /tmp/values.yaml
          setValues:
            - key: stageName
              value: prod
            - key: envType
              value: prod
            - key: appName
              value: grafana-taylor
          #valuesFiles:
          #- ${{ vars.path_src }}/config/prod/grafana-taylor-values.yaml
          #- ${{ vars.path_src }}/applications/grafana-taylor/values/prod.yaml
      - uses: helm-template
        config:
          releaseName: grafana
          namespace: taylor-monitoring
          buildDependencies: true
          includeCRDs: true
          path: ${{ vars.path_src }}/applications/grafana-taylor
          outPath: ${{ vars.path_out }}/deploy/grafana-taylor
          valuesFiles:
          - /tmp/values.yaml
      - task:
          name: push-manifests
        as: promotion
        vars:
        - name: asPR
          value: "true"
        - name: targetBranch
          value: env/prod/grafana-taylor
      - uses: argocd-update
        config:
          apps:
          - name: prod-grafana-taylor
            sources:
            - repoURL: ${{ vars.gitRepo }}
              desiredRevision: ${{ outputs.promotion.commit }}
---
# Source: application-generator/templates/stage.yaml
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-home-assistant
  namespace: homelab-apps
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: home-assistant
    sources:
      stages:
      - test-home-assistant
  promotionTemplate:
    spec:
      vars:
      - name: gitRepo
        value: https://github.com/syoder89-homelab/homelab-apps
      - name: targetBranch
        value: env/prod/home-assistant
      - name: path_src
        value: ./src
      - name: path_out
        value: ./out
      steps:
      - task:
          name: prepare-workdir 
      # The config-generator chart needs access to the config manifests
      # Copy config manifests into config-generator chart
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/platform
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/applications/home-assistant/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/application
      # Render the config files from templates
      - uses: helm-template
        config:
          releaseName: config-generator
          path: ${{ vars.path_src }}/charts/config-generator
          outLayout: flat
          outPath: /tmp/values.yaml
          setValues:
            - key: stageName
              value: prod
            - key: envType
              value: prod
            - key: appName
              value: home-assistant
          #valuesFiles:
          #- ${{ vars.path_src }}/config/prod/home-assistant-values.yaml
          #- ${{ vars.path_src }}/applications/home-assistant/values/prod.yaml
      - uses: helm-template
        config:
          releaseName: home-assistant
          namespace: home-assistant
          buildDependencies: true
          includeCRDs: true
          path: ${{ vars.path_src }}/applications/home-assistant
          outPath: ${{ vars.path_out }}/deploy/home-assistant
          valuesFiles:
          - /tmp/values.yaml
      - task:
          name: push-manifests
        as: promotion
        vars:
        - name: asPR
          value: "true"
        - name: targetBranch
          value: env/prod/home-assistant
      - uses: argocd-update
        config:
          apps:
          - name: prod-home-assistant
            sources:
            - repoURL: ${{ vars.gitRepo }}
              desiredRevision: ${{ outputs.promotion.commit }}
---
# Source: application-generator/templates/stage.yaml
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-influxdb-scott
  namespace: homelab-apps
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: influxdb-scott
    sources:
      stages:
      - test-influxdb-scott
  promotionTemplate:
    spec:
      vars:
      - name: gitRepo
        value: https://github.com/syoder89-homelab/homelab-apps
      - name: targetBranch
        value: env/prod/influxdb-scott
      - name: path_src
        value: ./src
      - name: path_out
        value: ./out
      steps:
      - task:
          name: prepare-workdir 
      # The config-generator chart needs access to the config manifests
      # Copy config manifests into config-generator chart
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/platform
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/applications/influxdb-scott/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/application
      # Render the config files from templates
      - uses: helm-template
        config:
          releaseName: config-generator
          path: ${{ vars.path_src }}/charts/config-generator
          outLayout: flat
          outPath: /tmp/values.yaml
          setValues:
            - key: stageName
              value: prod
            - key: envType
              value: prod
            - key: appName
              value: influxdb-scott
          #valuesFiles:
          #- ${{ vars.path_src }}/config/prod/influxdb-scott-values.yaml
          #- ${{ vars.path_src }}/applications/influxdb-scott/values/prod.yaml
      - uses: helm-template
        config:
          releaseName: influxdb
          namespace: scott-monitoring
          buildDependencies: true
          includeCRDs: true
          path: ${{ vars.path_src }}/applications/influxdb-scott
          outPath: ${{ vars.path_out }}/deploy/influxdb-scott
          valuesFiles:
          - /tmp/values.yaml
      - task:
          name: push-manifests
        as: promotion
        vars:
        - name: asPR
          value: "true"
        - name: targetBranch
          value: env/prod/influxdb-scott
      - uses: argocd-update
        config:
          apps:
          - name: prod-influxdb-scott
            sources:
            - repoURL: ${{ vars.gitRepo }}
              desiredRevision: ${{ outputs.promotion.commit }}
---
# Source: application-generator/templates/stage.yaml
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-kargo-config
  namespace: homelab-apps
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: kargo-config
    sources:
      stages:
      - test-kargo-config
  promotionTemplate:
    spec:
      vars:
      - name: gitRepo
        value: https://github.com/syoder89-homelab/homelab-apps
      - name: targetBranch
        value: env/prod/kargo-config
      - name: path_src
        value: ./src
      - name: path_out
        value: ./out
      steps:
      - task:
          name: prepare-workdir 
      # The config-generator chart needs access to the config manifests
      # Copy config manifests into config-generator chart
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/platform
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/applications/kargo-config/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/application
      # Render the config files from templates
      - uses: helm-template
        config:
          releaseName: config-generator
          path: ${{ vars.path_src }}/charts/config-generator
          outLayout: flat
          outPath: /tmp/values.yaml
          setValues:
            - key: stageName
              value: prod
            - key: envType
              value: prod
            - key: appName
              value: kargo-config
          #valuesFiles:
          #- ${{ vars.path_src }}/config/prod/kargo-config-values.yaml
          #- ${{ vars.path_src }}/applications/kargo-config/values/prod.yaml
      - uses: helm-template
        config:
          releaseName: kargo-config
          namespace: homelab-apps
          buildDependencies: true
          includeCRDs: true
          path: ${{ vars.path_src }}/applications/kargo-config
          outPath: ${{ vars.path_out }}/deploy/kargo-config
          valuesFiles:
          - /tmp/values.yaml
      - task:
          name: push-manifests
        as: promotion
        vars:
        - name: asPR
          value: "true"
        - name: targetBranch
          value: env/prod/kargo-config
      - uses: argocd-update
        config:
          apps:
          - name: prod-kargo-config
            sources:
            - repoURL: ${{ vars.gitRepo }}
              desiredRevision: ${{ outputs.promotion.commit }}
---
# Source: application-generator/templates/stage.yaml
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-mosquitto-taylor
  namespace: homelab-apps
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: mosquitto-taylor
    sources:
      stages:
      - test-mosquitto-taylor
  promotionTemplate:
    spec:
      vars:
      - name: gitRepo
        value: https://github.com/syoder89-homelab/homelab-apps
      - name: targetBranch
        value: env/prod/mosquitto-taylor
      - name: path_src
        value: ./src
      - name: path_out
        value: ./out
      steps:
      - task:
          name: prepare-workdir 
      # The config-generator chart needs access to the config manifests
      # Copy config manifests into config-generator chart
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/platform
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/applications/mosquitto-taylor/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/application
      # Render the config files from templates
      - uses: helm-template
        config:
          releaseName: config-generator
          path: ${{ vars.path_src }}/charts/config-generator
          outLayout: flat
          outPath: /tmp/values.yaml
          setValues:
            - key: stageName
              value: prod
            - key: envType
              value: prod
            - key: appName
              value: mosquitto-taylor
          #valuesFiles:
          #- ${{ vars.path_src }}/config/prod/mosquitto-taylor-values.yaml
          #- ${{ vars.path_src }}/applications/mosquitto-taylor/values/prod.yaml
      - uses: helm-template
        config:
          releaseName: mosquitto
          namespace: taylor-monitoring
          buildDependencies: true
          includeCRDs: true
          path: ${{ vars.path_src }}/applications/mosquitto-taylor
          outPath: ${{ vars.path_out }}/deploy/mosquitto-taylor
          valuesFiles:
          - /tmp/values.yaml
      - task:
          name: push-manifests
        as: promotion
        vars:
        - name: asPR
          value: "true"
        - name: targetBranch
          value: env/prod/mosquitto-taylor
      - uses: argocd-update
        config:
          apps:
          - name: prod-mosquitto-taylor
            sources:
            - repoURL: ${{ vars.gitRepo }}
              desiredRevision: ${{ outputs.promotion.commit }}
---
# Source: application-generator/templates/stage.yaml
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-mosquitto
  namespace: homelab-apps
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: mosquitto
    sources:
      stages:
      - test-mosquitto
  promotionTemplate:
    spec:
      vars:
      - name: gitRepo
        value: https://github.com/syoder89-homelab/homelab-apps
      - name: targetBranch
        value: env/prod/mosquitto
      - name: path_src
        value: ./src
      - name: path_out
        value: ./out
      steps:
      - task:
          name: prepare-workdir 
      # The config-generator chart needs access to the config manifests
      # Copy config manifests into config-generator chart
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/platform
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/applications/mosquitto/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/application
      # Render the config files from templates
      - uses: helm-template
        config:
          releaseName: config-generator
          path: ${{ vars.path_src }}/charts/config-generator
          outLayout: flat
          outPath: /tmp/values.yaml
          setValues:
            - key: stageName
              value: prod
            - key: envType
              value: prod
            - key: appName
              value: mosquitto
          #valuesFiles:
          #- ${{ vars.path_src }}/config/prod/mosquitto-values.yaml
          #- ${{ vars.path_src }}/applications/mosquitto/values/prod.yaml
      - uses: helm-template
        config:
          releaseName: mosquitto
          namespace: mosquitto
          buildDependencies: true
          includeCRDs: true
          path: ${{ vars.path_src }}/applications/mosquitto
          outPath: ${{ vars.path_out }}/deploy/mosquitto
          valuesFiles:
          - /tmp/values.yaml
      - task:
          name: push-manifests
        as: promotion
        vars:
        - name: asPR
          value: "true"
        - name: targetBranch
          value: env/prod/mosquitto
      - uses: argocd-update
        config:
          apps:
          - name: prod-mosquitto
            sources:
            - repoURL: ${{ vars.gitRepo }}
              desiredRevision: ${{ outputs.promotion.commit }}
---
# Source: application-generator/templates/stage.yaml
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-victoria-metrics-scott
  namespace: homelab-apps
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: victoria-metrics-scott
    sources:
      stages:
      - test-victoria-metrics-scott
  promotionTemplate:
    spec:
      vars:
      - name: gitRepo
        value: https://github.com/syoder89-homelab/homelab-apps
      - name: targetBranch
        value: env/prod/victoria-metrics-scott
      - name: path_src
        value: ./src
      - name: path_out
        value: ./out
      steps:
      - task:
          name: prepare-workdir 
      # The config-generator chart needs access to the config manifests
      # Copy config manifests into config-generator chart
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/platform
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/applications/victoria-metrics-scott/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/application
      # Render the config files from templates
      - uses: helm-template
        config:
          releaseName: config-generator
          path: ${{ vars.path_src }}/charts/config-generator
          outLayout: flat
          outPath: /tmp/values.yaml
          setValues:
            - key: stageName
              value: prod
            - key: envType
              value: prod
            - key: appName
              value: victoria-metrics-scott
          #valuesFiles:
          #- ${{ vars.path_src }}/config/prod/victoria-metrics-scott-values.yaml
          #- ${{ vars.path_src }}/applications/victoria-metrics-scott/values/prod.yaml
      - uses: helm-template
        config:
          releaseName: victoria-metrics-scott
          namespace: scott-monitoring
          buildDependencies: true
          includeCRDs: true
          path: ${{ vars.path_src }}/applications/victoria-metrics-scott
          outPath: ${{ vars.path_out }}/deploy/victoria-metrics-scott
          valuesFiles:
          - /tmp/values.yaml
      - task:
          name: push-manifests
        as: promotion
        vars:
        - name: asPR
          value: "true"
        - name: targetBranch
          value: env/prod/victoria-metrics-scott
      - uses: argocd-update
        config:
          apps:
          - name: prod-victoria-metrics-scott
            sources:
            - repoURL: ${{ vars.gitRepo }}
              desiredRevision: ${{ outputs.promotion.commit }}
---
# Source: application-generator/templates/stage.yaml
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-victoria-metrics-taylor
  namespace: homelab-apps
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: victoria-metrics-taylor
    sources:
      stages:
      - test-victoria-metrics-taylor
  promotionTemplate:
    spec:
      vars:
      - name: gitRepo
        value: https://github.com/syoder89-homelab/homelab-apps
      - name: targetBranch
        value: env/prod/victoria-metrics-taylor
      - name: path_src
        value: ./src
      - name: path_out
        value: ./out
      steps:
      - task:
          name: prepare-workdir 
      # The config-generator chart needs access to the config manifests
      # Copy config manifests into config-generator chart
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/platform
      - uses: copy
        config:
          inPath: ${{ vars.path_src }}/applications/victoria-metrics-taylor/config
          outPath: ${{ vars.path_src }}/charts/config-generator/config/application
      # Render the config files from templates
      - uses: helm-template
        config:
          releaseName: config-generator
          path: ${{ vars.path_src }}/charts/config-generator
          outLayout: flat
          outPath: /tmp/values.yaml
          setValues:
            - key: stageName
              value: prod
            - key: envType
              value: prod
            - key: appName
              value: victoria-metrics-taylor
          #valuesFiles:
          #- ${{ vars.path_src }}/config/prod/victoria-metrics-taylor-values.yaml
          #- ${{ vars.path_src }}/applications/victoria-metrics-taylor/values/prod.yaml
      - uses: helm-template
        config:
          releaseName: victoria-metrics
          namespace: taylor-monitoring
          buildDependencies: true
          includeCRDs: true
          path: ${{ vars.path_src }}/applications/victoria-metrics-taylor
          outPath: ${{ vars.path_out }}/deploy/victoria-metrics-taylor
          valuesFiles:
          - /tmp/values.yaml
      - task:
          name: push-manifests
        as: promotion
        vars:
        - name: asPR
          value: "true"
        - name: targetBranch
          value: env/prod/victoria-metrics-taylor
      - uses: argocd-update
        config:
          apps:
          - name: prod-victoria-metrics-taylor
            sources:
            - repoURL: ${{ vars.gitRepo }}
              desiredRevision: ${{ outputs.promotion.commit }}
