---
# Source: grafana-scott/charts/grafana/templates/tests/test.yaml
apiVersion: v1
kind: Pod
metadata:
  name: grafana-scott-test
  labels:
    helm.sh/chart: grafana-10.1.0
    app.kubernetes.io/name: grafana
    app.kubernetes.io/instance: grafana-scott
    app.kubernetes.io/version: "12.2.0"
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": "before-hook-creation,hook-succeeded"
  namespace: scott-monitoring
spec:
  serviceAccountName: grafana-scott-test
  containers:
    - name: grafana-scott-test
      image: "docker.io/bats/bats:v1.4.1"
      imagePullPolicy: "IfNotPresent"
      command: ["/opt/bats/bin/bats", "-t", "/tests/run.sh"]
      volumeMounts:
        - mountPath: /tests
          name: tests
          readOnly: true
  volumes:
    - name: tests
      configMap:
        name: grafana-scott-test
  restartPolicy: Never
