---
# Source: grafana-taylor/charts/grafana/templates/tests/test.yaml
apiVersion: v1
kind: Pod
metadata:
  name: test-grafana-test
  labels:
    helm.sh/chart: grafana-10.4.0
    app.kubernetes.io/name: grafana
    app.kubernetes.io/instance: test-grafana
    app.kubernetes.io/version: "12.3.0"
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": "before-hook-creation,hook-succeeded"
  namespace: test-taylor-monitoring
spec:
  serviceAccountName: test-grafana-test
  containers:
    - name: test-grafana-test
      image: "docker.io/bats/bats:v1.4.1"
      imagePullPolicy: "IfNotPresent"
      command: ["/opt/bats/bin/bats", "-t", "/tests/run.sh"]
      volumeMounts:
        - mountPath: /tests
          name: tests
          readOnly: true
  volumes:
    - name: tests
      configMap:
        name: test-grafana-test
  restartPolicy: Never
