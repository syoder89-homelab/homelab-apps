---
# Source: tasmota-monitor/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tasmota-monitor
  namespace: taylor-monitoring
  labels:
    app: tasmota-monitor
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: tasmota-monitor
  template:
    metadata:
      labels:
        app: tasmota-monitor
    spec:
      containers:
      - name: tasmota-monitor
        image: us-central1-docker.pkg.dev/personal-356214/tasmota-monitor/tasmota-monitor:20260410-9128f06
        imagePullPolicy: IfNotPresent
        env:
        - name: SENSOR
          value: "taylor_water_uv"
        - name: BROKER
          value: "tcp://mosquitto:1883"
        - name: VM_PUSH_URL
          value: "http://victoria-metrics-victoria-metrics-single-server:8428/api/v1/import/prometheus"
        - name: PROBE_ADDR
          value: ":8080"
        - name: OTLP_ENDPOINT
          value: "tempo.observability:4317"
        ports:
        - name: metrics
          containerPort: 8080
        startupProbe:
          httpGet:
            path: /startupz
            port: metrics
          failureThreshold: 30
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /healthz
            port: metrics
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /readyz
            port: metrics
          periodSeconds: 10
          failureThreshold: 3
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
          limits:
            memory: 64Mi
